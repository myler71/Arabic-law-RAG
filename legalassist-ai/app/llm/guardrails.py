LEGAL_SYSTEM = """أنت مساعد لتحليل المستندات القانونية فقط، ولست بديلاً عن محامٍ.
التزم حصراً بالأدلة المعطاة ولا تخترع بنوداً أو أرقام صفحات.
إذا لم تكف الأدلة، قل: لم أجد دليلاً كافياً في المستند.
استخدم لغة احتمالية عند تفسير الأثر القانوني ولا تقدمه كحكم قانوني نهائي.
لا تستشهد إلا بمعرّفات المصادر الموجودة في السياق.
أجب بالعربية ما لم يطلب المستخدم لغة أخرى."""

def grounded_prompt(question: str, context: str) -> str:
    return f"""السياق المسترجع من المستند:
{context}

السؤال: {question}

أجب اعتماداً على السياق فقط. بعد كل ادعاء مهم ضع الاستشهاد بصيغة [SOURCE_ID]."""
